// AUTOGENERATED by scripts/codegen/generate.py — do not edit.
// Regenerate by editing InterfaceManifest.yml and rebuilding.
//
// D-Bus signal registration + emission entry points. Implemented in
// DBusSignals.gen.cpp; called from DBusBackend::run() and ::stop().

#ifndef ZWAVED_DBUS_SIGNALS_GEN_HPP
#define ZWAVED_DBUS_SIGNALS_GEN_HPP

#include "DBusBackend.hpp"

#include <sdbus-c++/IObject.h>

namespace ExternalApi
{

/// Register every D-Bus signal declared in InterfaceManifest.yml on
/// the given sdbus object. Call from DBusBackend::run() before
/// finishRegistration().
auto registerGeneratedSignals(sdbus::IObject& obj) -> void;

/// Subscribe to every MessageBus event referenced by a manifest
/// signal's `triggered_by:`. Each subscriber emits the corresponding
/// D-Bus signal(s). For signals with `decode:`, the codec call and
/// std::nullopt guard are emitted automatically. Subscriptions are
/// appended to impl.generatedSignalSubs.
auto subscribeGeneratedSignals(DBusBackend::Impl& impl) -> void;

/// Release every subscription added by subscribeGeneratedSignals().
/// Call from DBusBackend::stop() before releasing the cache-update
/// subscribers.
auto unsubscribeGeneratedSignals(DBusBackend::Impl& impl) -> void;

}  // namespace ExternalApi

#endif  // ZWAVED_DBUS_SIGNALS_GEN_HPP
