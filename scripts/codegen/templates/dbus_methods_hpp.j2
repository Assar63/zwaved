// AUTOGENERATED by scripts/codegen/generate.py — do not edit.
// Regenerate by editing InterfaceManifest.yml and rebuilding.
//
// Forward declarations for the daemon's hand-written `custom:`
// method handlers, plus the entry point invoked from
// DBusBackend::run() to register every D-Bus method on the object.

#ifndef ZWAVED_DBUS_METHODS_GEN_HPP
#define ZWAVED_DBUS_METHODS_GEN_HPP

#include "DBusBackend.hpp"

#include <cstdint>
#include <string>
#include <vector>

#include <sdbus-c++/IObject.h>
#include <sdbus-c++/Types.h>

namespace ExternalApi
{

{% for method in manifest.dbus.methods if method is custom %}
auto {{ custom_handler(method) }}(DBusBackend::Impl& impl
{%- for p in method.params -%}
    , {{ p | cpp_param_decl }}
{%- endfor -%}
) -> {{ method | method_return }};
{% endfor %}

/// Register every D-Bus method declared in InterfaceManifest.yml on
/// the given sdbus object. Bodies for `publish:` / `publish_constant:`
/// are inlined; `custom:` methods are forwarded to the hand-written
/// handlers declared above.
auto registerGeneratedMethods(sdbus::IObject& obj, DBusBackend::Impl& impl) -> void;

}  // namespace ExternalApi

#endif  // ZWAVED_DBUS_METHODS_GEN_HPP
