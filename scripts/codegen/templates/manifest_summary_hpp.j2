// AUTOGENERATED by scripts/codegen/generate.py — do not edit.
// Regenerate by editing InterfaceManifest.yml and rebuilding.
//
// Phase 1 sanity-check header. Counts the top-level sections of the
// manifest so a build that links zwaved proves the codegen pipeline
// is wired end to end (YAML → loader → schema → Jinja2 → file).
// Replaced by real targets in later phases.

#ifndef ZWAVED_MANIFEST_GEN_HPP
#define ZWAVED_MANIFEST_GEN_HPP

namespace ManifestSummary
{
constexpr int VERSION       = {{ manifest.version }};
constexpr int STRUCT_COUNT  = {{ struct_count }};
constexpr int EVENT_COUNT   = {{ event_count }};
constexpr int METHOD_COUNT  = {{ method_count }};
constexpr int SIGNAL_COUNT  = {{ signal_count }};
constexpr int CC_COUNT      = {{ cc_count }};
}  // namespace ManifestSummary

#endif  // ZWAVED_MANIFEST_GEN_HPP
