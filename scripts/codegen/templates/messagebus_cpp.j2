// AUTOGENERATED by scripts/codegen/generate.py — do not edit.
// Regenerate by editing InterfaceManifest.yml and rebuilding.
//
// Explicit instantiation of MessageBus::subscribe<T> and
// MessageBus::publish<T> for every event declared in
// InterfaceManifest.yml. Template definitions live in
// src/message-bus/MessageBusInternal.hpp; this file only triggers
// the per-T instantiation so each event has linkable symbols.

// IWYU pragma: keep — explicit instantiations need the template
// bodies visible, but no symbol from this header is named directly
// below.
#include "MessageBusInternal.hpp"  // IWYU pragma: keep

namespace MessageBus
{
{% for ev in manifest.events %}
template auto subscribe<{{ ev.name }}>(std::function<void(const {{ ev.name }}&)>) -> SubscriptionId;
template auto publish<{{ ev.name }}>(const {{ ev.name }}&) -> void;
{% endfor %}
}  // namespace MessageBus
